interface Props {
}

export default function AiHeader(props: Props) {
  return (
    <header
    className="w-full flex justify-between items-center gap-5">
      <button
      type="button">

      </button>
    </header>
  )
}